%h1 Listing tour_guides

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @tour_guides.each do |tour_guide|
      %tr
        %td= link_to 'Show', tour_guide
        %td= link_to 'Edit', edit_tour_guide_path(tour_guide)
        %td= link_to 'Destroy', tour_guide, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Tour guide', new_tour_guide_path
