%h1 Listing tourists

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @tourists.each do |tourist|
      %tr
        %td= link_to 'Show', tourist
        %td= link_to 'Edit', edit_tourist_path(tourist)
        %td= link_to 'Destroy', tourist, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Tourist', new_tourist_path
